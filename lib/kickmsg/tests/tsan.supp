# No suppressions needed.
# The seqlock data races were fixed by the refcount pin in try_receive()
# and the two-phase commit (LOCKED_SEQUENCE) in publish().
# This file is kept empty so the suppression infrastructure remains
# available if needed in the future.
