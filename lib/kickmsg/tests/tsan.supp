# Seqlock-protected slot data: publisher writes payload, subscriber reads
# via memcpy with sequence-number validation (double-check before/after).
# TSan cannot reason about seqlock correctness.
race:kickmsg::Publisher::send
race:kickmsg::Subscriber::try_receive
race:kickmsg::Subscriber::try_receive_view
