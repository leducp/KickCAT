if (NUTTX)
    if (NUTTX_ARCH_CHIP STREQUAL "xmc4")
        add_subdirectory(nuttx/xmc4800)
    else()
        add_subdirectory(nuttx/lan9252)
    endif()
endif()

if (RPI)
    add_subdirectory(rpi/lan9252)
endif()
